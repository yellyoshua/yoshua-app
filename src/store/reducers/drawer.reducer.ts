// import {DrawerTypes} from '../types';

const initialState = {
  open: false,
};

export const drawer = (state = initialState, action: any) => {
  switch (action.type) {
    default:
      return state;
  }
};
